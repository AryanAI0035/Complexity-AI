# Complexity-AI
A Machine Learning tool that uses NLP (TF-IDF &amp; Random Forest) to statically analyze C source code and predict Big-O time complexity ($O(1)$, $O(n)$, $O(n^2)$) with 90%+ accuracy.
